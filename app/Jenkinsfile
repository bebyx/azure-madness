#!/usr/bin/env groovy

node {
    checkout scm

    stage('Build') {
        def appImage = docker.build("app:${env.BUILD_ID}",
                                    "-f ./app/Dockerfile ./app")
    }
}